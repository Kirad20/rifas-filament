// Importación de AOS
import 'https://cdnjs.cloudflare.com/ajax/libs/aos/2.3.4/aos.js';

// Inicializar AOS cuando el documento esté listo
document.addEventListener('DOMContentLoaded', function() {
    AOS.init({
        duration: 800,
        once: true
    });
});
